{
  "name": "ucgo-core",
  "version": "0.1.0",
  "description": "Core protocol primitives for UCGO — packet encoding/decoding, transport crypto, binary I/O, and dispatcher",
  "author": "Gerard McGuire",
  "license": "ISC",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx/esm --test src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
